apiVersion: v1
kind: Service
metadata:
  name: airflow-webserver-service
  namespace: airflow
spec:
  selector:
    tier: airflow
    component: webserver
  ports:
  - protocol: TCP
    port: 30000
    targetPort: 8080
    nodePort: 30000
  type: LoadBalancer
  loadBalancerIP: 10.0.2.3
